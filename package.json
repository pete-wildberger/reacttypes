{
  "name": "reacttypes",
  "version": "1.0.0",
  "description": "testing react with typescript",
  "main": "server.js",
  "author": "Peter Wildberger",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "updatedep": "cd client && yarn install",
    "buildup": "cd client && yarn run build"
  },
  "dependencies": {
    "axios": "^0.17.0",
    "body-parser": "^1.18.2",
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "querystring": "^0.2.0"
  }
}
